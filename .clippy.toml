# Clippy configuration
# See: https://rust-lang.github.io/rust-clippy/master/index.html#configuration

# Enforce maximum cognitive complexity per function
cognitive-complexity-threshold = 25

# unwrap banned in lib code via #![deny(clippy::unwrap_used)] in lib.rs
# Removed from disallowed-methods to avoid false positives in test code
